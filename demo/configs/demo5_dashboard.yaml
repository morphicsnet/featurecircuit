# Demo 5 Investor Dashboard configuration
runs:
  baseline_dir: 'runs/baseline'
  ensemble_dir: 'runs/ensemble'
  spike_hypergraph_dir: 'runs/spike_hypergraph'
  causal_dir: 'runs/causal'

selection:
  mode: 'latest'  # 'latest' or 'manual'
  manual_paths:
    baseline: null
    ensemble: null
    spike_hypergraph: null
    causal: null

plots:
  hist_bins: 30
  top_k_stii: 20
  top_k_gender_nodes: 10

toggles:
  show_baseline: true
  show_ensemble: true
  show_hypergraph: true
  show_causal: true